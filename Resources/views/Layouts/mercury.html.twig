<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, maximum-scale=1.0, initial-scale=1.0">
    <title>{{page.title}}</title>
    <meta name="page-id" content="{{page.id}}"/>
    <meta name="mercury-images" content="{{path('koala_content_mercury_images')}}"/>
    <meta name="mercury-content" content="{{path('koala_content_mercury_content', {page_id: page.id})}}">
    <meta name="mercury-new" content="{{path('koala_content_page_new')}}"/>
    <meta name="mercury-edit" content="{{path('koala_content_page_edit', {page_id: page.id})}}"/>
    <link rel="stylesheet" href="{{asset('mercury/stylesheets/mercury.bundle.css')}}" type="text/css" charset="utf-8"/>
    <link rel="stylesheet" href="{{asset('bundles/koalacontent/css/mercury_plugin.css')}}" type="text/css" charset="utf-8"/>
    <link rel="stylesheet" href="{{asset('bundles/koalacontent/css/flash.css')}}" type="text/css" charset="utf-8"/>
    <script src="{{asset('https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js')}}" type="text/javascript" charset="utf-8"></script>
    <script type="text/javascript" src="{{ asset('bundles/fosjsrouting/js/router.js') }}"></script>
    <script type="text/javascript" src="{{ path('fos_js_routing_js', {"callback": "fos.Router.setData"}) }}"></script>
    <script src="{{asset('mercury/javascripts/mercury.min.js')}}" type="text/javascript" charset="utf-8"></script>
    <script src="{{asset('mercury/javascripts/mercury_dialogs.js')}}" type="text/javascript" charset="utf-8"></script>
    <script src="{{asset('bundles/koalacontent/js/mercury_plugin.js')}}" type="text/javascript" charset="utf-8"></script>
</head>
<body>
    <script type="text/javascript">
        // Set to the url that you want to save any given page to, leave null for default handling.
        var saveUrl = "{{path('koala_content_mercury_content', {page_id: page.id})}}";

        // Instantiate the PageEditor
        new Mercury.PageEditor(saveUrl, {
            saveStyle:  'json', // 'form', or 'json' (default json)
            saveMethod: '{{save_method|upper}}', // 'PUT', or 'POST', (create, vs. update -- default PUT)
            visible:    false  // boolean - if the interface should start visible or not
        });
    </script>
    <div id="flash-messages"></div>
</body>
</html>
